import { NotificationInterceptor } from './notification.interceptor';

describe('NotificationInterceptor', () => {
  it('should be defined', () => {
    expect(new NotificationInterceptor()).toBeDefined();
  });
});
