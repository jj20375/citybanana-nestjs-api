export enum DatingDemandStatus {
    // 開放報名中
    STAT_RECRUITING = 0,
    // 報名額滿
    STAT_FULL = 1,
    // 報名截止
    STAT_CLOSED = 2,
    // 活動取消
    STAT_CANCELLED = -1,
    // 活動終止
    STAT_TERMINATED = -2,
}
